config {
    type: "incremental",
    schema: "dataform"
}

SELECT
  meal_id,
  meal_name,
  calories,
  '${dataform.projectConfig.vars.execution_date}' AS execution_date
FROM
  ${ref("meals")}
WHERE
  date = '${dataform.projectConfig.vars.execution_date}'
